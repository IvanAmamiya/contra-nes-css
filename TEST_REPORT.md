# 魂斗罗 1 · 原版首关 v1.2.2 测试报告

2026-09-09，Windows，Node.js v24.14.1，Codex 内置 Chromium 浏览器。游戏逻辑为 JavaScript，画面为 DOM + CSS。

## 自动化结果

**138 / 138 通过，0 失败，0 跳过**，发布前重跑约 3.07 秒。本次执行全部规则、渲染、输入、素材和通关测试。原始输出：[test-results.txt](test-results.txt)。

| 检查 | 范围与结果 |
|---|---|
| 原版地图解码 | 13 屏，每屏 56 个 super-tile；3328×224；47 段地面，包含两座各四段的断桥 |
| 原版对象 | 30 条记录，含重复生成；18 个普通敌人、7 个胶囊/补给箱、2 座桥触发器、3 个碉堡部件 |
| 出场规则 | 按原表卷轴触发；尚未出场的对象不参与命中；胶囊从左侧进入并向右飞行，双胶囊为 R/L |
| 地图像素 | 全部 728 个 32×32 场景块，对照源图 (512,16,3328,224) 裁剪逐像素验证；61 种去重 CSS 样式一致 |
| 复活 | 每隔 8 像素抽样，覆盖卷轴 0–3072 的 385 个位置；复活后停留 90 帧，均未落入深坑 |
| 规则与碰撞 | 开始、暂停、重开、移动、跳跃、趴下、潜水、下穿、断桥、单向卷轴、扫掠命中、阵营及同帧多弹保护 |
| 武器与生命 | N/M/S 弹数限制、M 连射、H 节流、八方向、R 弹速、F 轨迹、L 四段延迟、道具、B 护罩、死亡、复活、奖命 |
| 输入集成 | DOM 替身运行真实 app.js；帧间快速点按、多指输入、取消/丢失捕获、失焦与暂停后清键 |
| 渲染生命周期 | 2000 次绘制复用节点；暂停冻结动作；重开释放场景；销毁断开观察器 |
| 素材 | 233 个精灵文件/236 帧、17 张地图/参考图、3986 个背景图块，文件哈希与 CSS 可见像素一致 |
| 原始 CHR | 3404 个图案、7836 个配色组合；NES 2bpp 位平面与透明色处理 |

## 压力和完整通关

120 轮 × 6000 帧，共 **720,000 模拟帧**。每帧检查有限坐标、速度、时间和分数；生命、卷轴边界、实体数量保持有效。峰值为 19 颗子弹和 20 个普通敌人。相当于 60 Hz 下累计 200 分钟的加速规则模拟，不是浏览器连续运行 200 分钟的实测。

10 个种子均在 **30 命练习模式**下通过正常按键完成整关，耗时 4219–4597 模拟帧（约 70.3–76.6 秒），剩余 21–24 条生命。每条路线都包含道具拾取、敌人击毁、完整卷轴与核心击破。驾驶员不改位置、武器或敌人状态；测试启动使用游戏已有的 30 命选项。

真实浏览器发布前再次运行同样 10 个种子，**10/10 通关**，批量模拟 264 ms；该耗时不代表实际渲染帧率。[浏览器结果](browser-test-results.json)。默认三命路线另行验证 GAME OVER 和重新开始；未证明三命无伤通关或原版难度等价。

## 纯 CSS 实际渲染

以下为 v1.2.2 发布前重新进行的真实浏览器测量，包含朝向和弹种修正。

| 场景 | 实际 rAF 帧 | 耗时 | 平均 FPS | 帧间隔 P95 | JS 绘制提交 P95 | 游戏区节点峰值 |
|---|---:|---:|---:|---:|---:|---:|
| 普通速度前十秒 | 600 | 10017 ms | 59.8 | 16.8 ms | 0.3 ms | 134 |
| 全地图滚动至通关 | 600 | 9984 ms | 60.0 | 16.8 ms | 0.4 ms | 144 |

普通测试每个显示帧推进一个 60 Hz 逻辑步；全地图测试每帧推进十步，覆盖 76.62 秒游戏时间与最终卷轴 3072。后者检验快速滚动、场景切换和节点复用，不能当作全程按原速游玩 77 秒的性能测量。JS 提交时间不包含浏览器后续布局、栅格化与合成。

游戏仅复用 70 个镜头附近的场景块节点。两轮检查 Canvas、img 元素均为 0；回归页启动的位图资源请求为 0，控制台无脚本错误。完整原始记录：[普通速度](css-render-performance.json)、[全地图](css-full-map-performance.json)。机器负载与设备会影响帧率。

## 实际页面检查

发布前另以 `npm run build` 生成的独立静态目录启动服务复查：首页开始、手机射击与暂停正常；390×844 和 320×740 无横向溢出；素材页显示全部 233 张精灵卡，无脚本错误。静态输出不包含 Git 目录、依赖目录或构建用原始 PNG/GIF，HTML 本地引用均存在。

本地 HTTP 服务另做 6 项冒烟检查：首页和素材页返回 200，隐藏目录（含编码路径）返回 403，缺失页面返回 404，全部通过。

- 桌面：开始、暂停、恢复；丛林、碉堡、通关画面；[丛林截图](game-preview.png)。
- 390×844：触屏按钮出现，开始/射击/暂停可用，页面宽度和 scrollWidth 均为 390；[手机截图](mobile-preview.png)。
- 320×740：无横向溢出，触屏控件宽 296；画面仍为 70 个场景块节点、0 个 Canvas/img/SVG。
- 素材页：233 张精灵卡、17 个地图/参考图选项正常载入。游戏、回归页、素材页均未见脚本错误。

未在物理手机上实测多点触控，其他浏览器和长期音频运行未覆盖。内置浏览器限制 file://；实际验证的运行方式是 npm start 后访问本地 HTTP。项目使用相对 JS/CSS，包内资源齐全，离线入口为 index.html。

## 本轮修正与边界

首关从自定义短路线换为原版十三屏布局，并接入原始地面/水域采样和固定对象记录。核对了对象 X 字节是卷轴触发位置、飞行胶囊会重设到左侧、属性 4 对应 L 等细节。修复测试驾驶员因趴下改变外框而误判位置、关底镜头差一像素、未出场对象提前参与子弹命中的问题。

仍是单人首关 JavaScript 实现。敌人多阶段 AI、随机增援、断桥时序、碉堡炮弹/破坏动画、部分武器细节及调色板循环有简化；后七关玩法、双人和原版音乐未实现。素材完整性只针对列出的来源，未证明穷尽整份 ROM 的所有图形状态。[完整异同](SOURCE_COMPARISON.md)。

## v1.2.1 敌人方向修正

素材库中的玩家姿态默认朝右，跑兵六帧和固定枪手姿态默认朝左。原先共用玩家的镜像符号，导致向左移动的敌人外观却朝右。现按素材原始朝向计算镜像，保留角色中心和移动路线；固定枪手会面向玩家所在侧，与子弹方向一致，位置和射击周期不变。

新增三项回归，覆盖跑兵左右方向 × 六帧动作 × 两个卷轴位置、枪手朝向与子弹方向、玩家原有镜像、翻转后角色中心保持一致。全部 133 项通过，包含 720,000 帧压力模拟和 10 次正常输入通关。浏览器另跑 10 次通关，全部通过，无脚本错误；回归页增加“回放跑兵”以直接核对画面。[方向修正截图](enemy-direction-preview.png)。

## v1.2.2 S 枪表现与速度修正

新增五项回归：普通/R 五发原始速度值、八方向旋转与镜像、槽位不足优先中心弹及点射限制、S 实际生成子弹在第 0/15/16/31/32/33 帧的 CSS 精灵、M/普通敌弹/炮弹素材区分。全部 138 项通过，含 720,000 帧随机压力和 10 次完整通关。

浏览器另跑 10 次均通关，无脚本错误。“回放 S 枪”通过正常移动、射击和拾取获得 S，只开一组并在飞行第 32 帧截图，实际 DOM 有五颗大散弹，Canvas/img/SVG 均为 0。[S 枪截图](spread-preview.png)。完整通关采用 30 命模式，规则时序与原版的全部碰撞细节尚未逐帧等价。
