# v2.0.0 精神 FC 版测试记录

验证日期：2026-09-09。规则测试使用 Node.js 24.14.1；画面及声音检查使用桌面应用内 Chromium 浏览器。

## 自动化规则与素材检查

`node --test tests/*.test.cjs`：**197 / 197 通过，0 失败**。完整输出见 `spirits-test-results.txt`。

- 保留原 NES 碰撞、敌人朝向、五颗散弹、地图原像素、原首关十种子完整通关等回归。
- 扩展三关各十种随机种子，只输出正常按键完整通关；M/S/H/C/L/F 武器、冷却、弹量、追踪、范围伤害、穿透、双槽与死亡惩罚。
- 30 命/3 命、选关与下一关、炸弹、墙面/天花板攀附、双枪旋转、定点瞄准、暂停、失焦、快速点按与触屏取消、设置持久化。
- 原 NES 720,000 帧随机压力 + 新模式 360,000 帧；所有坐标保持有限，实体有界。扩展随机压力观测最多 15 颗子弹 / 7 个敌人，机制上限 120 颗子弹。
- 从实际 CSS 独立重建三幅 1280×224 画面，验证输出 SHA-256；检查每个 16×16 区块最多四种颜色、颜色属于对应属性调色板、2×2 纹理简化一致。
- 四种 BRR 音源转换可重现；输出 7-bit 范围正确、相邻样本变化不超过 2。1,000 次声音事件中最多五声部，暂停/静音后释放全部声音节点。

## 浏览器验证

三关 × 十种子：**30 / 30 通关通过**。初次跨三关 600 帧测试：约 **59.8 FPS**，渲染函数 P95 **0.5 ms**，帧间隔 P95 **16.8 ms**，最多 **136 DOM 节点**。图块、平台、Boss、所有枪弹均由 CSS 绘制；**0 Canvas、0 img/SVG、0 位图请求**。

已目视检查城市蓝/赭黄、工厂灰/赭黄、巢穴紫/赭黄场景及三个关底。音频可启用，AudioContext 为 running；停止后活动声音节点归零。声音检查验证播放链路与生命周期，不是原硬件声学保真认证。

最终复核仍为 30/30 通关；600 帧复核平均 55.9 FPS、渲染 P95 0.4 ms、帧间隔 P95 18.1 ms、最多 136 节点，期间也加载了手机布局页。保留这次结果而不替换为较快的一次。音频确认四个采样缓冲区均已实际创建，停止后活动声部为 0。390 像素 iframe 内选关、生命设置与全部 11 个触屏操作均可见，未见横向溢出。

最终构建的浏览器输出保存在 `spirits-browser-results.json`、`spirits-performance.json` 与 `spirits-audio-results.json`；390 像素同源 iframe 的响应式检查见 `spirits-mobile-results.json`。桌面应用的直接视口覆盖没有改变后台标签的实际尺寸，因此手机布局通过独立 iframe 复核；不是手机真机测试。

性能数据是当前桌面环境的观测，不能代表所有手机。跨关性能测试每显示帧推进 16 个规则帧，用于覆盖整条路线；不会把这当作实际游玩的速度。没有测试俯视关卡、双人、载具或真实 FC/SNES 硬件，因为此版未实现这些功能。
