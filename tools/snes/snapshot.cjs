const fs=require('fs'),path=require('path');
function snapshot(file){const data=fs.readFileSync(file),header=data.toString('ascii',0,14);if(header!=='#!s9xsnp:0014\n')throw Error('Unsupported snapshot '+header);let at=14;const blocks={};while(at+11<=data.length){const name=data.toString('ascii',at,at+3),len=Number(data.toString('ascii',at+4,at+10));if(!/^[A-Z0-9]{3}$/.test(name)||!Number.isSafeInteger(len))break;at+=11;if(at+len>data.length)throw Error('Truncated');blocks[name]=data.subarray(at,at+len);at+=len;}return blocks;}
function ppu(b){let i=0;function num(n){const v=b.readUIntBE(i,n);i+=n;return v;}const vma={};for(const[k,n]of Object.entries({high:1,increment:1,address:2,mask:2,count:2,shift:2,wram:4}))vma[k]=num(n);const bg=Array.from({length:4},()=>({map:num(2)*2,x:num(2),y:num(2),size:num(1),tiles:num(2)*2,mapSize:num(2)}));const mode=num(1),priority=num(1);i+=4;const palette=Array.from({length:256},()=>num(2));const sprites=Array.from({length:128},()=>({x:num(2),y:num(2),flipX:num(1),flipY:num(1),tile:num(2),priority:num(1),palette:num(1),large:num(1)}));i+=3;const obj={base:num(2),select:num(2),sizes:num(1)};return {vma,bg,mode,priority,palette,sprites,obj};}
module.exports={snapshot,ppu};
